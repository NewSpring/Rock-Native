// This should be able to be called from tests but currently
// it is not working.

// import { curry, fromPairs, map, adjust, toPairs } from "ramda";
// import R from "recompose";
//
// const mapValues = curry((fn, obj) =>
//   fromPairs(map(adjust(fn, 1), toPairs(obj))),
// );
//
// const withJest = mapValues(fn => jest.fn(fn));
//
console.log("hai");
// export default withJest(R);
